({
  // rerender : function(component, helper) {
  //   this.superRerender();
  //   let recordTypeId = component.get("v.recordId").state.recordId;
  //   component.set("v.recordId", recordTypeId);
 // }
})